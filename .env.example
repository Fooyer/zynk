# Token de um Personal Access Token (fine-grained) do GitHub, com permissão
# "Contents: Read and write" só no repositório Fooyer/zynk-tauri — é o que
# scripts/publish-release.js usa pra criar a release e subir os instaladores.
# Copie este arquivo para ".env" (git-ignorado) e preencha de verdade.
GH_TOKEN=
